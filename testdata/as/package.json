{
  "name": "hello_wapc",
  "version": "1.0.0",
  "description": "AssemblyScript waPC example",
  "scripts": {
    "build": "asc assembly/index.ts -b hello.wasm --use abort=assembly/index/abort --optimize"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@wapc/as-guest": "^v0.2.1"
  },
  "devDependencies": {
    "assemblyscript": "Note: assemblyscript version must match @wapc/as-guest",
    "assemblyscript": "^0.17.14"
  }
}
